local options = {
  ensure_installed = { "lua", "vim", "vimdoc" },
  parser_install_dir = nil,
  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
