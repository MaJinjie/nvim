;; extends

((identifier) @namespace.builtin
  (#eq? @namespace.builtin "vim"))

((identifier) @namespace.builtin
  (#eq? @namespace.builtin "User"))

((identifier) @namespace.builtin
  (#lua-match? @namespace.builtin "^Mini%u%l+$"))
