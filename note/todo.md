1. "file_browser" 修改窗格的高度
2. "zoxide" edit spilt vspilt ctrl-e 使用heo-tree打开，而不是默认的nerw
甚至可以考虑将zoxide的搜索作为一个tmux的命令来绑定，自动分割窗口和打开nvim



















# bindkey 
1. R可以被配置为在行尾部进入插入模式，输入文本，按esc,返回之前的光标
